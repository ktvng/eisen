fn main() {
    let var x, var y, var z: bool
    x = true or false
    y = x and false
    z = not y

    print("%i %i %i", x, y, z)
}

/// [Test]
/// name = "logical"
/// info = """\
///     tests for simple logical operators
/// """
/// [Expects]
/// success = true
/// match_case = false
/// output = "true false true"
