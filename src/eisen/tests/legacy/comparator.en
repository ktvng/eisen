fn main() {
    let w, x, y, z: bool
    w = 3 >= 4
    x = 4 < 12
    y = 4 > 5
    z = 4 <= 4
    print("%i %i %i %i", w, x, y, z)
}

/// [Test]
/// name = "comparator"
/// info = """\
///     Tests for simple comparisons
/// """
/// [Expects]
/// success = true
/// match_case = false
/// output = "false true false true"
