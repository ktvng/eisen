fn main() {
    let x, y, z = 1, 8, 10
    x = x + y + z
    z = (2 * z) - (x / 1)
    y = 0 - y
    print("%i %i %i", x, z, y)
}

/// [Test]
/// name = "arithmetic"
/// info = """\
///     Tests for simple arithmetric operations.
/// """
/// [Expects]
/// success = true
/// output = "19 1 -8"
