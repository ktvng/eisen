fn main() {
    var? x: int
    x = nil
    let z = 43
    x = z
    if(true) {
        x + 4
    }
}

/// [Test]
/// name = "e_niluse"
/// info = """\
///     tests for simple embeddings
/// """
/// [Expects]
/// success = false
/// [[Expects.Exceptions]]
/// type = "NilUsageException"
/// contains = ">> 7"