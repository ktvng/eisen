fn main() {
    let var a, var b, var c, var d = 4, 4, 4, 4
    a += 10
    b /= 10
    c -= 10
    d *= 10
    print("%i %i %i %i", a, b, c, d)
}

/// [Test]
/// name = "compound"
/// info = """\
///     tests for simple compound operators
/// """
/// [Expects]
/// success = true
/// output = "14 0 -6 40"
