// #1 multiple assign
fn maths(x: int, y: int) -> sum: int, diff: int {
    sum = x + y
    diff = x - y
}

// fn points(x: int, y: int*) -> a: int, b: int* {
//     a = x
//     b = y
// }

fn main() {
    let x, y = 4, 45
    let a, b = maths(x, y)
    // var n: int
    // val m: int
    // let c, d = points(4, 4)
}