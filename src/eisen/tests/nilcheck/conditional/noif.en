struct obj {
    x: int

    create(x: int) -> self: obj {
        self.x = x
    }
}

fn main() {
    let a = obj(7)
    var? p = a
    print("%i", p.x)
}

/// [Test]
/// name = "nilcheck/conditional/if"
/// info = "Nil check is required to narrow a type"

/// [Expects]
/// success = false

/// [[Expects.Exceptions]]
/// type = "NilUsageException"
/// contains = ">> 12"
